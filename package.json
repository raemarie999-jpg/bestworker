{
  "name": "minutetemp-dallas-worker",
  "version": "1.0.0",
  "description": "MinuteTemp Dallas Weather Intelligence — background worker + dashboard",
  "main": "worker.js",
  "scripts": {
    "start": "node worker.js",
    "dev": "MT_API_KEY=${MT_API_KEY} node worker.js",
    "serve": "npx http-server . -p 3000 -o dashboard.html"
  },
  "dependencies": {
    "axios": "^1.6.0",
    "node-cron": "^3.0.3"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
